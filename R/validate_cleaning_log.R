validate_cleaning_log <- function(cleaning_log,
                                  var_names_col,
                                  values_col,
                                  survey,
                                  tool) {
  # TODO: test that all variables are valid questions in the tool
  # TODO: test that for each id-variable combination is unique
  # TODO: test that for numeric questions, the new value is a valid number or NA
}
